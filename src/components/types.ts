interface gamecard{
    title: string;
    description: string;
    price: number;
    category: string;
    image: string;
}

interface Iavatar {
    image: string;

}

interface IheaderDetails{
    title: string;
    description: string;
}

interface IpageHeader{
    image: string;
    title: string;
    description: string;
}

interface Iviewallbtn{
    name: string;
}

interface IslideCartCard{
    image?: string;
    price?: number;
    name?: string;
}